[build]
  command = "npm run build"
  publish = "dist"
  base = "."

[build.environment]
  NODE_VERSION = "20"

# SPA Fallback: Redirect all traffic to index.html so React Router handles it
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
